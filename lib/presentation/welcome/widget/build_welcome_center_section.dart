import 'package:flutter/material.dart';

class BuildWelcomeCenterSection extends StatelessWidget {
  const BuildWelcomeCenterSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
      flex: 3,
      child: Container(),
    );
  }
}
