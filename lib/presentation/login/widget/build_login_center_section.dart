import 'package:flutter/material.dart';

class BuildLoginCenterSection extends StatelessWidget {
  const BuildLoginCenterSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(),
    );
  }
}
